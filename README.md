# Student Grade Manager 📚

A simple Python command-line application to manage student grades using JSON as a database.

## Features
- Calculate GPA automatically.
- Save student data to a JSON file.
- Search for students by name.

## How to Run
1. Run `main.py` to add a student.
2. Run `search_student.py` to find a student.